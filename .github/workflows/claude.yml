name: Claude AI Assistant

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  issues:
    types: [opened, assigned]

# 동일 이슈에서 중복 실행 방지
concurrency:
  group: claude-${{ github.event.issue.number || github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  claude-response:
    # @claude 멘션이 있을 때만 실행
    if: contains(github.event.comment.body, '@claude') || contains(github.event.issue.body, '@claude')
    runs-on: ubuntu-latest
    timeout-minutes: 10

    permissions:
      contents: write
      issues: write
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Hugo
        run: |
          wget -O hugo.deb https://github.com/gohugoio/hugo/releases/download/v0.152.2/hugo_extended_0.152.2_linux-amd64.deb
          sudo dpkg -i hugo.deb
          rm hugo.deb

      - name: Install Claude Code
        run: npm install -g @anthropic-ai/claude-code

      - name: Run Claude
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          COMMENT_BODY: ${{ github.event.comment.body || github.event.issue.body }}
        run: |
          set -euo pipefail

          # 이슈/PR 코멘트에서 @claude 이후 내용 추출
          PROMPT=$(echo "$COMMENT_BODY" | sed 's/.*@claude//')

          # Claude 실행 (파일 생성/수정 허용, CI 환경에서 권한 자동 승인)
          echo "🤖 Claude 실행 중..."
          if RESPONSE=$(claude -p "$PROMPT" --max-turns 30 --dangerously-skip-permissions 2>&1); then
            CLAUDE_STATUS="success"
          else
            CLAUDE_STATUS="error"
            RESPONSE="Claude 실행 중 오류가 발생했습니다:\n\n\`\`\`\n${RESPONSE}\n\`\`\`"
          fi

          # 응답 길이 제한 (GitHub 코멘트 최대 65536자)
          if [ ${#RESPONSE} -gt 60000 ]; then
            RESPONSE="${RESPONSE:0:60000}

          ...(응답이 너무 길어 일부가 생략되었습니다)"
          fi

          # 변경사항 확인 및 PR 생성
          if [ "$CLAUDE_STATUS" = "success" ] && [ -n "$(git status --porcelain)" ]; then
            # 브랜치 생성
            BRANCH_NAME="claude/issue-${ISSUE_NUMBER}-$(date +%Y%m%d%H%M%S)"
            git checkout -b "$BRANCH_NAME"

            git add .
            git commit -m "feat: Claude가 이슈 #${ISSUE_NUMBER} 요청 처리

          🤖 Generated by Claude Code"
            git push -u origin "$BRANCH_NAME"

            # PR 생성
            PR_URL=$(gh pr create \
              --title "Claude: 이슈 #${ISSUE_NUMBER} 요청 처리" \
              --body "## 관련 이슈
          Closes #${ISSUE_NUMBER}

          ## 변경 사항
          ${RESPONSE:0:30000}

          ---
          *🤖 Generated by Claude Code*" \
              --base main \
              --head "$BRANCH_NAME")

            RESULT_MSG="✅ PR이 생성되었습니다: $PR_URL"
          elif [ "$CLAUDE_STATUS" = "error" ]; then
            RESULT_MSG="❌ 오류가 발생했습니다. 로그를 확인해주세요."
          else
            RESULT_MSG="ℹ️ 파일 변경사항이 없습니다."
          fi

          # 결과를 코멘트로 작성
          gh issue comment ${ISSUE_NUMBER} --body "## Claude 응답

          $RESPONSE

          ---
          $RESULT_MSG

          *🤖 Generated by Claude Code*"
