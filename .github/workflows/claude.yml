name: Claude AI Assistant

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  issues:
    types: [opened, assigned]

jobs:
  claude-response:
    # @claude 멘션이 있을 때만 실행
    if: contains(github.event.comment.body, '@claude') || contains(github.event.issue.body, '@claude')
    runs-on: ubuntu-latest

    permissions:
      contents: write
      issues: write
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Hugo
        run: |
          wget -O hugo.deb https://github.com/gohugoio/hugo/releases/download/v0.152.2/hugo_extended_0.152.2_linux-amd64.deb
          sudo dpkg -i hugo.deb

      - name: Install Claude Code
        run: npm install -g @anthropic-ai/claude-code

      - name: Run Claude
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # 이슈/PR 코멘트에서 @claude 이후 내용 추출
          COMMENT_BODY="${{ github.event.comment.body || github.event.issue.body }}"
          PROMPT=$(echo "$COMMENT_BODY" | sed 's/.*@claude//')

          # Claude 실행 (파일 생성/수정 허용)
          RESPONSE=$(claude -p "$PROMPT" --max-turns 10 2>&1) || true

          # 변경사항 확인 및 커밋
          if [ -n "$(git status --porcelain)" ]; then
            git add .
            git commit -m "feat: Claude가 이슈 #${{ github.event.issue.number }} 요청 처리

            🤖 Generated by Claude Code"
            git push

            COMMIT_MSG="파일이 생성/수정되어 커밋했습니다."
          else
            COMMIT_MSG=""
          fi

          # 결과를 코멘트로 작성
          gh issue comment ${{ github.event.issue.number }} --body "## Claude 응답

          $RESPONSE

          $COMMIT_MSG

          ---
          *🤖 Generated by Claude Code*"
